import styled from '@emotion/styled';

import { CoverWrapper } from 'widgets/ui/NavBar/styled';

export const PlayListInfo = styled.div`
  display: flex;
  flex: 1;
  align-items: center;
  gap: 30px;
`;

export const PlayListCoverWrapper = styled(CoverWrapper)``;
