import { MainLayout } from 'widgets/ui/';

import { DetailPage } from './ui/';

const Page = () => {
  // const params = useParams();

  return (
    <MainLayout>
      <DetailPage />
    </MainLayout>
  );
};

export default Page;
