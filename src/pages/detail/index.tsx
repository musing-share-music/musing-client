import { MainLayout } from 'shared/ui';
import { DetailPage } from 'shared/ui/Detail';

const Page = () => {
  // const params = useParams();

  return (
    <MainLayout>
      <DetailPage />
    </MainLayout>
  );
};

export default Page;
