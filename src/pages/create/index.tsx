import { MainLayout } from 'shared/ui';
import { CreatePage } from 'shared/ui/Create';

const Page = () => {
  return (
    <MainLayout>
      <CreatePage />
    </MainLayout>
  );
};

export default Page;
