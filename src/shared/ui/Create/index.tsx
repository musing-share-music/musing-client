import { CreateForm } from './CreateForm';
import { Container, Layout } from './styled';

export const CreatePage = () => {
  return (
    <Layout>
      <Container>
        <CreateForm />
      </Container>
    </Layout>
  );
};
